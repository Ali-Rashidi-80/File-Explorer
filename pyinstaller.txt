# Portable onefile (recommended) — same as build.bat / build_portable.bat
python -m PyInstaller --noconfirm FileExplorer.portable.spec

# Output:
#   dist\FileExplorer_Portable.exe

# Docs: docs/BUILD.md  |  docs/BUILD.fa.md  |  README.md
